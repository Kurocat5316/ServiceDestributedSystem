The home page ip number is: http://144.6.227.101/

On Access File will be the file used to execute file

The Input File will be the file being used to execute.
Require Date allow user to select the date they want to get the file.

Then, When user click upload, the files of above will upload server and a passcode will be generated

Users can check their file status on first passcode text box. After click submit, it will show the processing status.

Meanwhile, if process already be done, users can download the output file.

Second passcode textbox is used to Cancel job. When user input passcode and click Cancel button, his job will be stopped
and the passcode will be check-out on server.